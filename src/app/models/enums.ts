export enum ItemType {
  none,
  image,
  text,
  element,
}

export enum ItemStatus {
  none,
  image_selected,
  image_preset,
  image_filter,
  image_crop,
  text_selected,
  text_edit,
  text_font_list,
  text_effect,
  element_selected,
}
